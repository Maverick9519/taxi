[Unit]
Description=Taxi Backend (FastAPI)
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/opt/taxi_service
EnvironmentFile=/opt/taxi_service/.env
ExecStart=/opt/taxi_service/venv/bin/uvicorn taxi_prod:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
